# WHALE Docker stack - copy to .env and set values
MYSQL_ROOT_PASSWORD=change_me_strong_password
MYSQL_DATABASE=wordpress
MYSQL_USER=wordpress
MYSQL_PASSWORD=change_me_wp_password

GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=change_me_grafana_password
GRAFANA_ROOT_URL=https://grafana.yourdomain.com

# Optional: Let's Encrypt for new projects (create_project.sh)
LETSENCRYPT_EMAIL=admin@yourdomain.com

# Uptime Kuma - auto-add HTTP monitors when creating sites (create_project.sh → uptime_kuma.py)
# Panel: http://localhost:3001 — create admin user, then Settings → API Tokens → create token
UPTIME_KUMA_URL=http://localhost:3001
# UPTIME_KUMA_TOKEN=your_api_token_from_settings

# Alternative: use login (no token). Script will use these if token is not set.
# UPTIME_KUMA_USER=admin
# UPTIME_KUMA_PASSWORD=your_admin_password
